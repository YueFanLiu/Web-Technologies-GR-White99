server:
  port: 9192

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres}
    username: ${DB_USERNAME:postgres.mbtrnkskilrkzvqqqpbi}
    password: ${DB_PASSWORD:uY83VHkkj8SwrPU2}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
